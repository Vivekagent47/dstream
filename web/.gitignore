node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# bun is the only package manager here — never commit npm/pnpm/yarn lockfiles
package-lock.json
pnpm-lock.yaml
yarn.lock
